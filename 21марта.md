# Сводка работ за 21 марта 2026

## ✅ Что сделали

### 1. Исправлен GitHub Actions (синхронизация тендеров)
- **Проблема**: sync-tenders.js падал с exit code 1 — поле `source` было missing
- **Исправление**: добавлено поле `source: feedSource` в mapItem
- Расширены RSS-фиды: bicotender (main + building_materials + construction) + РТС-тендер + zakaz.gov.ru
- Отказоустойчивый цикл: каждый фид в try/catch, дедупликация по external_id
- Пакетные upserts по 50 записей

### 2. Рассылки с фото, ссылкой и кнопкой
- Страница `/admin/broadcast` — поля: текст, URL фото, кнопка (текст + URL)
- Страница `/admin` (BroadcastKeyboardButton) — полноценный модальный UI с превью
- API принимает photoUrl, linkLabel, linkUrl
- bot.ts: добавлена функция `sendPhoto()`

### 3. Исправлены названия площадок
- **Проблема**: все площадки кроме Bico показывались как "Zakupki.gov"
- **Исправление**: используем `FILTER_PLATFORMS.find(p => p.id === s)?.label` вместо хардкода

### 4. Анализ рынка — выбор категории
- Теперь при нажатии "📊 Анализ рынка" сначала открывается клавиатура выбора категории
- Кнопка "📋 Общий анализ (все категории)" всегда первая
- Показываются категории пользователя + кнопка "Все категории"
- Анализ берёт данные только по выбранным площадкам и регионам пользователя

### 5. Исправлена AI-галлюцинация
- **Проблема**: Mistral придумывал тендеры, организации, даты из 2024
- **Исправление**: в промпт добавлена текущая дата + строгий запрет "НЕ придумывай тендеры, НЕ упоминай конкретные организации"

### 6. Расширены категории и регионы
- FILTER_CATEGORIES: 14 → 40+ (полная таксономия: ЖБИ, Газобетон, Арматура, Кровля, Сантехника, Кабель, и т.д.)
- FILTER_REGIONS: добавлены Республика Дагестан, Дербент, Махачкала, всего 25+ регионов
- Кнопки "✅ Выбрать все" / "☑️ Снять все" в фильтрах категорий и регионов

### 7. Исправлен фильтр площадок (критический баг)
- **Проблема**: когда пользователь убирал Bico и выбирал РТС/OTC — показывались тендеры с Bico
- Причина: тихий фолбэк на все площадки когда выбранные пустые
- **Исправление**: фолбэк убран, возвращается `{ tenders, noDataFromSources }`
- Если `noDataFromSources = true` → показываем сообщение "По выбранным площадкам данных пока нет"

### 8. ИИ Чат — поиск реальных тендеров
- `handleAiChatMessage` определяет поисковый intent (найди, покажи, тендер по...)
- Извлекает категории из FILTER_CATEGORIES и регионы (+ короткие формы: дагестан, питер, казань...)
- Запускает `searchTendersByText()` — прямой ILIKE без фильтра площадок
- Показывает реальные карточки тендеров с кнопками

### 9. Свободный текст вне ИИ-чата
- **Проблема**: любой текст вне AI-режима пытался искать тендеры
- **Исправление**: теперь показывает предложение открыть 🤖 ИИ Чат с примерами

---

## ⚠️ Что ещё не работает / не доработано

### 1. Площадки кроме Bico — нет данных в БД
- RSS-фиды РТС-тендер и zakaz.gov.ru подключены в sync-tenders.js
- **Но**: эти URL могут не работать или не возвращать нужные данные
- В базе сейчас только bicotender
- Нужно проверить: работают ли RSS-фиды РТС и zakupki в реальности

### 2. FILTER_PLATFORMS в keyboards.ts содержит площадки без данных
- Пользователь может выбрать OTC, Сбербанк-АСТ, ЕЭТП — но этих данных нет в БД
- Нет парсеров для этих площадок
- Пользователь видит "По выбранным площадкам данных пока нет" — это честно, но неудобно

### 3. ИИ-чат: fallback когда категория не распознана
- Если пользователь пишет "найди тендеры по стеклу" — категория "стекло" не в FILTER_CATEGORIES
- `extractCategories()` вернёт [] → поиск не запустится, пойдёт в обычный AI-чат
- Нужно: более умное извлечение с синонимами или GPT-парсинг

### 4. Команды /favorites, /inwork — пагинация
- Показывает только первые 5, есть пагинация (кнопка "Показать ещё")
- Нужно проверить что кнопка работает в callbacks.ts

### 5. Digest/уведомления — не тестировались
- `src/lib/digest/service.ts` обновлён под новый return type
- Автоматические рассылки через cron не проверялись

### 6. Admin panel — страница статистики
- Есть `/admin` с кнопкой рассылки
- Нет отображения статистики пользователей, подписок

### 7. sync-tenders.js — переменная `bicotenderId` не определена
- Строка 216: `raw_payload: { title, link, category, pubDate, bicotenderId }`
- `bicotenderId` не объявлена в mapItem — нужно заменить на `tenderId`

### 8. TypeScript — могут быть ошибки
- Изменён return type `getRelevantTendersForUser` — callers обновлены но не проверены билдом
- Нужно запустить `npm run build` и исправить ошибки

---

## Приоритеты на следующую сессию

1. **Исправить** `bicotenderId` → `tenderId` в sync-tenders.js (строка 216)
2. **Проверить** `npm run build` — нет ли TS-ошибок
3. **Проверить** реально ли работают RSS-фиды РТС и zakupki.gov.ru
4. **Расширить** `extractCategories()` в aichat.ts синонимами для редких материалов
5. **Добавить** статистику в admin panel
